# Changes since your GitHub repo (v2.1 → v2.2.0)

Baseline: the `flatsplit-main-2.zip` you downloaded from GitHub on 2026-09-05.
Everything below was built **on top of that exact code** — your `App.jsx`,
`vite.config.js`, and Personal-tab group-spend feature are preserved
unchanged. Nothing was deleted.

## Modified files (14)

| File | What changed |
|---|---|
| `src/styles.css` | v3 primitives (avatar, chip), row layouts, mobile-native hardening, bounding system, activity meta pill + 1.5:1 ratio, 3D tab bar |
| `src/components/Balances.jsx` | Spending-summary card removed; avatars on net balances and settle rows; owe/owed coloring |
| `src/components/Activity.jsx` | Category chip leads each expense row |
| `src/components/Settle.jsx` | Payer → payee avatars on suggestions |
| `src/components/Personal.jsx` | Category chips on rows; month-by-month totals as fixed-size boxes (your group-spend logic intact) |
| `src/components/Insights.jsx` | Overview stats use compact lakh-proof format |
| `src/components/DonutChart.jsx` | Center label/value bounded in JS (SVG can't ellipsize) |
| `src/lib/balances.js` | `fmtMoneyShort()` added (nothing existing changed) |
| `src/lib/statement.test.js` | Tests for `fmtMoneyShort` |
| `src/demo/mockSupabase.js` | `.in()` filter support; demo seeds for lakh-scale + long-description cases |
| `package.json` | version 2.2.0; Capacitor deps; `cap:*` scripts |
| `package-lock.json` | regenerated by npm install |
| `CHANGELOG.md` | 2.1.0 (your changes, documented) + 2.2.0 entries |
| `README.md` | Pointer to docs/ |

## New files (6 + native projects)

- `src/components/Avatar.jsx`, `src/lib/avatar.js`
- `src/components/CategoryChip.jsx`, `src/lib/categoryMeta.js`
- `capacitor.config.json`
- `docs/DESIGN.md`, `docs/MOBILE_APPS.md`, `docs/CHANGES_SINCE_GITHUB.md` (this file), `docs/v2.2.0-since-github.patch`
- `android/`, `ios/` — Capacitor native projects (commit them; their copied
  web-asset folders are excluded and regenerated by `npx cap sync`)

## Database

**No new migration.** Everything in 2.2.0 is frontend-only. Your Supabase is
already at v6 and stays there.

## Two ways to bring this into your repo

**A) Upload the zip contents** (GitHub UI or copy over your clone) — same
as before; overwriting is safe because nothing was deleted.

**B) Apply the patch** (if you prefer surgical):
```bash
cd your-flatsplit-clone
git checkout -b v2.2-ui
git apply docs/v2.2.0-since-github.patch   # modified + new text files
# then copy in the android/ and ios/ folders from the zip
npm install
npm test && npm run build
```
The patch excludes `package-lock.json` (npm regenerates it) and the native
folders (binary-ish trees; copy them in).

## If your repo moved on after 2026-09-05

Re-download your latest repo zip and hand it over — I'll rebase these changes
onto it and regenerate this manifest and patch.
